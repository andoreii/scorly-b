# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Xcode — user-specific & build artifacts
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xcscheme

# Build output
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# Generated by XcodeGen — do not commit
ScorlyB.xcodeproj/

# Local-only build settings — populated by `make gen`, never committed.
# Holds Supabase URL/key and (later) observability DSNs. See Local.xcconfig.example.
Local.xcconfig

# Custom Info.plist — generated by xcodegen from project.yml.
Scorly/Info.plist

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# CocoaPods (not used)
Pods/

# Carthage (not used)
Carthage/Build/
Carthage/Checkouts/

# fastlane (not used)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Editors
.idea/
.vscode/
*.swp
*~

# Design preview workspace (entire viz directory — reference material, not the iOS app)
viz/

# Reference / inspiration material (kept locally, never committed)
REFERENCE PHOTOS FOR UI UX BRUTALISM/
reference/
references/
Reference/
References/
inspiration/
Inspiration/
*.reference
*.reference.*

# AI assistant context files (kept locally, never committed)
CLAUDE.md
AGENTS.md
.claude/
.cursor/
.cursorrules
.aider*
.continue/
.codeium/
GEMINI.md

# Project docs (kept locally, never committed)
PRODUCT.md
DESIGN.md
GIT.md
