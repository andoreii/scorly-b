/// Courses feature — list + editor for the user's golf-course archive.
/// Cards mirror the History ticket-row idiom: hairline-bordered tickets
/// with mono metadata and a hero numeric (rounds played) anchored to
/// the right. Add / edit / delete actions live on a sibling editor
/// screen, not in a modal sheet (DESIGN.md bans modals as a default).
public enum ScorlyFeatureCourses {}
