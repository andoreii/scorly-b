/// Authentication feature. Exports `AuthGateView`, `BrutalistAuthView`,
/// and `AuthLoadingView`. The brutalist sign-in form is bare-bones
/// (email + password, no signup, no forgot-password) — those land in a
/// later phase.
public enum ScorlyFeatureAuth {}
