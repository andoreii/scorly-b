/// Trends feature — brutalist statistical instrument over the last
/// 10 or 20 completed rounds. Pure read-only: aggregates from the
/// rounds repository, renders compositions, never mutates state.
public enum ScorlyFeatureStats {}
